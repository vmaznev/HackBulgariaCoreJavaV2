
public class User {
    private String username;
    private String password;
    
    public User(String username, String password) {
        this.username = username; 
        if(this.username == "") {
            throw new DatabaseCorruptedException("Invalid Username...1");  
        }
        this.password = password;
    }
    
}
